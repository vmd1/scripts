$uninstallKeys = @('HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall', 'HKCU:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall'); $browsers = @('Opera', 'Firefox', 'Brave'); foreach ($key in $uninstallKeys) { if (Test-Path $key) { Get-ChildItem -Path $key | ForEach-Object { $displayName = $_.GetValue('DisplayName'); $uninstallString = $_.GetValue('UninstallString'); if ($displayName -and ($browsers -contains $displayName)) { Write-Output "$displayName found. Uninstaller path: $uninstallString" } } } }; Write-Output 'Listing all installed applications and their uninstallers:'; foreach ($key in $uninstallKeys) { if (Test-Path $key) { Get-ChildItem -Path $key | ForEach-Object { $displayName = $_.GetValue('DisplayName'); $uninstallString = $_.GetValue('UninstallString'); if ($displayName) { Write-Output "$displayName - Uninstaller path: $uninstallString" } } } }; Read-Host -Prompt 'Press Enter to exit'
